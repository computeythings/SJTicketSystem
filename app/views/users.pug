include header.pug
body
  h1= heading

  table.table.table-striped
    tr
      th(scope='col') Username
      th(scope='col') Admin
    each user in users
      tr
        td= user.username
        td= user.admin ? 'Yes' : 'No'

  a.btn.btn-primary(href='/users/add') Add
