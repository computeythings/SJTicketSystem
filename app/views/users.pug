include header.pug
body
  h1= heading

  table.users
    tr
      th Username
      th Admin
    each user in users
      tr
        td= user.username
        td= user.admin ? 'Yes' : 'No'

  a(href='/users/add') Add
