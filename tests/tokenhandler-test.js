"user strict"
const assert = require('assert');
const tokens = require('../src/tokenhandler.js');

describe('tokenhandler.js', () => {
  describe('generateRefreshToken', () => {
    it('should successfully generate a refresh token given a username');
  });
});
